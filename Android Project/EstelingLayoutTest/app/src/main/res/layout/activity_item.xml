<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    tools:context=".Play2Activity">

    <ImageView
        android:id="@+id/gwb_avatar"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:scaleType="centerCrop"
        app:layout_constraintDimensionRatio="3f"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="34dp"
        android:layout_marginStart="8dp"
        android:text="Naam"
        app:layout_constraintBottom_toBottomOf="@+id/gwb_avatar"
        app:layout_constraintEnd_toEndOf="@+id/gwb_avatar"
        app:layout_constraintStart_toStartOf="@+id/gwb_avatar"
        app:layout_constraintTop_toTopOf="@+id/gwb_avatar" />

</android.support.constraint.ConstraintLayout>